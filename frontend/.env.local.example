# Stellar
NEXT_PUBLIC_STELLAR_NETWORK=testnet
NEXT_PUBLIC_STELLAR_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
NEXT_PUBLIC_FLASHPAY_CONTRACT_ID=        # deployed contract ID
NEXT_PUBLIC_USDC_CONTRACT_ID=GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5

# AI APIs (server-side only — no NEXT_PUBLIC_ prefix)
GROQ_API_KEY=                            # from console.groq.com — free

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=           # read-only
SUPABASE_SERVICE_ROLE_KEY=               # write — server-side only

# Monitoring
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=

# Payment
PAYEE_PUBLIC_KEY=                        # your Stellar wallet — receives tool payments
FEE_SPONSOR_SECRET_KEY=                  # server-side only — pays tx fees for users

# Stellar CLI / CI
STELLAR_SECRET_KEY=                      # deployer keypair — CI secrets only
